<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="28dp">

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.6" />

        <ImageView
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:src="@drawable/art_logo"
            android:importantForAccessibility="no" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/app_title"
            android:textColor="@color/gold"
            android:textSize="30sp"
            android:textStyle="bold" />

        <!-- Best score chip -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:background="@drawable/panel_card"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="18dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_trophy"
                app:tint="@color/gold"
                android:importantForAccessibility="no" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="@string/best"
                android:textColor="@color/text_dim"
                android:textSize="13sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/menuBest"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="@string/zero"
                android:textColor="@color/text_light"
                android:textSize="16sp"
                android:textStyle="bold" />
        </LinearLayout>

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.5" />

        <!-- NEW GAME -->
        <LinearLayout
            android:id="@+id/btnNew"
            style="@style/MenuRow"
            android:background="@drawable/menu_btn_green">

            <ImageView style="@style/MenuRowIcon" android:src="@drawable/ic_play" />

            <TextView style="@style/MenuRowText" android:text="@string/menu_new" />
        </LinearLayout>

        <!-- CONTINUE -->
        <LinearLayout
            android:id="@+id/btnContinue"
            style="@style/MenuRow"
            android:layout_marginTop="14dp"
            android:background="@drawable/menu_btn_gold">

            <ImageView
                style="@style/MenuRowIcon"
                android:src="@drawable/ic_undo"
                app:tint="@color/text_dark" />

            <TextView
                style="@style/MenuRowText"
                android:text="@string/menu_continue"
                android:textColor="@color/text_dark" />
        </LinearLayout>

        <!-- SETTINGS -->
        <LinearLayout
            android:id="@+id/btnSettings"
            style="@style/MenuRow"
            android:layout_marginTop="14dp"
            android:background="@drawable/menu_btn_dark">

            <ImageView style="@style/MenuRowIcon" android:src="@drawable/ic_cog" />

            <TextView style="@style/MenuRowText" android:text="@string/menu_settings" />
        </LinearLayout>

        <!-- EXTRA CONTENT -->
        <LinearLayout
            android:id="@+id/btnExtra"
            style="@style/MenuRow"
            android:layout_marginTop="14dp"
            android:background="@drawable/menu_btn_purple">

            <ImageView style="@style/MenuRowIcon" android:src="@drawable/ic_book" />

            <TextView style="@style/MenuRowText" android:text="@string/menu_extra" />
        </LinearLayout>

        <Space
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_version"
            android:textColor="@color/text_dim"
            android:textSize="12sp" />
    </LinearLayout>
</FrameLayout>
